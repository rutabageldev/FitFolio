# Development & Testing Dependencies
# Install these in addition to requirements.txt for local development

-r requirements.txt

# Testing
pytest==8.2.1
pytest-asyncio>=0.23.0
httpx==0.27.0

# Code Quality (for local dev container use)
# Note: pre-commit hooks use their own versioned tools, these are for manual use
ruff==0.5.6
mypy>=1.10.0

# Type stubs for better type checking
types-passlib>=1.7.0

# Pre-commit hooks framework
pre-commit>=3.5.0
